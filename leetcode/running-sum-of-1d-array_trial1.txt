class Solution:
    def runningSum(self, nums: List[int]) -> List[int]:
        prev = nums[0]
        for curr in range(1, len(nums)):
            nums[curr] += prev
            prev = nums[curr]

        return nums
        